package frontend;
	/**
	 * Class which will implement command which is the basic unit of SLogo commands. 
	 * Used in back-end. Kept here now as a placeholder so that the interfaces can compile.
	 */
public class Command {

}
